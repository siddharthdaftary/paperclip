<%= form_with(model: @favorite, local: true) do |f| %>

  <%= f.hidden_field :favoriter_id, value: @user.id %>

  <div class="form-group">
    <%= f.label :article_id %>

    <%= f.collection_select :article_id, Article.all, :id, :to_s, {}, class: "form-control" %>
  </div>

  <%= f.submit "Create favorite", class: "btn btn-block btn-outline-secondary" %>
<% end %>
